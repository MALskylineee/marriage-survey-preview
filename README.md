# Marriage compatibility questionnaire — preview

A throwaway, single-file preview used to test questionnaire usability with a
small group of volunteers. It runs entirely in the browser: it stores answers
only in the visitor's own browser (localStorage) and lets them download their
own result file. **Nothing is ever uploaded or transmitted** — there is no
server, no form submission, no analytics, no tracking.
